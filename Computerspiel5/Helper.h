#pragma once

#include "Game.h"

UInt random_uint(
	const UInt &lbound,
	const UInt &ubound);

Position random_position(
	const UInt &lbound1,
	const UInt &ubound1,
	const UInt &lbound2,
	const UInt &ubound2);